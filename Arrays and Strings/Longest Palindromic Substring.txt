class Solution {
    public String rev(String x){
        int l=x.length();
        String w="";
        for(int i=0;i<l;i++){
            w=x.charAt(i)+w;
        }
        return w;
    }
    public String longestPalindrome(String s) {
        String srev=rev(s);
        
        
    }
}


Remember the approach is by reversing the initial string and finding the longest common substring in both